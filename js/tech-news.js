const FEED_URL='data/tech-news.json';let articles=[];let category='ALL';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){const feed=document.getElementById('news-feed');const items=category==='ALL'?articles:articles.filter(a=>a.category===category);if(!items.length){feed.innerHTML='<div class="news-empty">NO SIGNALS MATCH THIS FILTER.</div>';return}feed.innerHTML=items.slice(0,40).map(a=>`<article class="news-card"><div class="news-meta"><div class="news-source">${esc(a.source)}</div><div>${esc(a.category)}</div><div>${esc(a.age||'RECENT')}</div></div><div><h3><a href="${esc(a.url)}" target="_blank" rel="noopener noreferrer">${esc(a.title)}</a></h3><p class="news-summary">${esc(a.summary||'Read the original report for full details.')}</p></div><a class="news-open" href="${esc(a.url)}" target="_blank" rel="noopener noreferrer">OPEN ↗</a></article>`).join('')}
async function load(){const status=document.getElementById('feed-status');try{const r=await fetch(FEED_URL,{cache:'no-store'});if(!r.ok)throw Error('feed unavailable');const d=await r.json();articles=Array.isArray(d.articles)?d.articles:[];status.textContent=`${articles.length} SIGNALS // FEED ONLINE`;document.getElementById('feed-updated').textContent=d.generated_at?`// UPDATED ${new Date(d.generated_at).toLocaleString()}`:'';render()}catch(e){status.textContent='FEED OFFLINE';document.getElementById('news-feed').innerHTML='<div class="news-empty">FEED DATA UNAVAILABLE. CHECK BACK AFTER THE NEXT SYNC.</div>'}}
document.querySelectorAll('[data-category]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-category]').forEach(x=>x.classList.remove('active'));b.classList.add('active');category=b.dataset.category;render()}));load();
